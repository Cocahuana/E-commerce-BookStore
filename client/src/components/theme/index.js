import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
	fonts: {
		heding: 'Poppins',
		body: 'Poppins',
	},
	colors: {
		brand: {
			pepe: '#64c2e4',
		},
	},
});

export default theme;
